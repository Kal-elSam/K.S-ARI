# Variables Sentry para la API (proyecto separado del dashboard en sentry.io).
# En Railway: añadir en el servicio. En local: suelen ir en el .env raíz del repo (../../.env).
SENTRY_DSN=
# Opcional si quieres forzar la etiqueta (si no, se usa RAILWAY_ENVIRONMENT o NODE_ENV).
# SENTRY_ENVIRONMENT=production
